"""Static scene read side: prefill token interpretation (scene_tokens),
most-recent-header recovery (scene_headers), queryable map facts
(scene_facts), assembled into the SceneIndex (scene_index)."""
