"""WAD assets compiled to lookup tables: raw WAD decoding (wad_assets),
the compiled asset banks (asset_banks), in-graph lookup accessors
(assets), HUD and weapon graphics (hud_assets, weapon_assets),
light-level colormaps (lighting), and the texture-coordinate PWL
wrap banks for the pixel pass (pwl_banks — texture math, not
lighting)."""
